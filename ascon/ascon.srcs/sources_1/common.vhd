package Common is   

   type blocktype is (Nonce, AData, Message, Tag);
   type Opcodes is (Init, applyRound, applyAD, applyDec, applyKeyI, applyKeyF, applyOne, NOP);

end Common;

package body Common is
   



end Common;